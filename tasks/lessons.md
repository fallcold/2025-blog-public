## 2026-02-27 编码事故复盘：中文被写成 `?`

- 问题：在命令里使用 `PowerShell here-string | node` 写中文内容，结果中文被替换为 `?`。
- 根因：中文通过管道传给原生命令时，终端代码页发生降级转换，字符丢失。
- 规则：以后写中文正文/配置，禁止使用 `... | node` 传输正文；统一使用 `apply_patch` 或 `WriteAllText(UTF-8)` 直接写文件。
- 验证：写入后必须做程序化检查，确认包含中文字符（`[\u4e00-\u9fff]`）且不是异常大量 `?`。
